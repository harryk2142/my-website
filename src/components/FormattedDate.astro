---
export interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time data-pagefind-sort="date" datetime={date.toISOString()}>
  {
    date.toLocaleDateString("de-de", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
